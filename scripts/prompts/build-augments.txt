英雄:{{name}}({{title}}),定位:{{tags}},技能机制摘要:{{skillSummary}}。
该英雄现有数据:胜率 {{winRate}}({{tier}})。

给出该英雄"{{buildName}}"方案的海克斯强化推荐,字段:augments(数组,每个含 name/tier/priority/winRate/reason)。

海克斯候选池(来自数据,仅从中筛选并分级):
{{augmentPool}}

分级规则:
- 核心必拿(3–6条):必抢,显著影响该英雄强度/质变点
- 情境可选(3–6条):看阵容/打法再选,特定条件下强力
- 慎选避开(2–4条):与该英雄契合度低或负收益

每条 reason 必须写成真实、具体、有决策价值的说明,说明"为什么拿、什么情况不拿",禁止模板拼接。

严格输出 JSON 数组(仅 augments 字段内容)。
